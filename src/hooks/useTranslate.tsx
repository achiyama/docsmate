import { useEffect, useState } from "react"

export const useTranslate = () => {
  const [count, setCount] = useState(0)

  useEffect(() => {
    // const getCurrentUrl = chrome.tabs.query({
    //   lastFocusedWindow: true,
    //   active: true
    // })
    const f = async () => {}
  }, [])
}
